<!--index.html.erb-->

<h1>投稿機能</h1>

<% @lists.each do |list| %>

  <h2>タイトル</h2>
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  
  <p>画像</p>
  <%= image_tag list.image, size: "200x200" %>
  
<% end %>

<h4><%= link_to "新規作成", new_list_path %></h4>